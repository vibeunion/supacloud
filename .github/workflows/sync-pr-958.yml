name: Sync PR 958 with main

on:
  push:
    branches:
      - automation/sync-supacloud-dependencies
    paths:
      - .github/workflows/sync-pr-958.yml

permissions:
  contents: write

concurrency:
  group: sync-pr-958-main
  cancel-in-progress: false

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout dependency synchronization branch
        uses: actions/checkout@v7
        with:
          ref: automation/sync-supacloud-dependencies
          fetch-depth: 0

      - name: Merge latest main
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git merge --no-edit origin/main
          git push origin HEAD:automation/sync-supacloud-dependencies
