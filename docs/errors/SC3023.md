<!-- Generated by scripts/check_diagnostic_docs.ts. Edit the generator when changing this contract. -->
# SC3023: duplicate-route-response-selector

## Meaning

The compiler emitted this diagnostic for duplicate route response selector. The declaration or generated contract violates a statically checkable SupaCloud rule.

## Fix

Correct the route path, parameter binding, schema, or redirect associated with `duplicate-route-response-selector`. Keep request and response contracts explicit and status-specific.

## Reproduction

Place the example below in a focused compiler fixture and assert that `SC3023` is reported with a file location. The fixture must fail before the fix and pass after it.

## Failing Example

```ts
// Expected diagnostic: SC3023
@Get("/items", { responses: { 200: ItemResponse, "2XX": ItemResponse } })
list() {}
```

## Registry

- Diagnostic names: `duplicate-route-response-selector`
- Owner: `compiler`
- Documentation URL: https://supacloud.dev/errors/SC3023
