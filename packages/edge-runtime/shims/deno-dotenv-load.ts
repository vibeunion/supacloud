// Deno dotenv/load.ts shim
// Bun handles .env files automatically, so this is just a no-op to prevent import errors.
export function config() {}
export function configSync() {}
export function load() {}
export function loadSync() {}
