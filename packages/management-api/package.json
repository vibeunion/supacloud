{
  "name": "@supacloud/management-api",
  "version": "0.3.23",
  "description": "SupaCloud Multi-tenant Management API",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:init": "bun run src/db/init.ts",
    "tsc": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "prebuild": "bun run scripts/pack-assets.ts && cd ../web-console && bun install && bun run build",
    "build:amd64": "bun build src/index.ts --compile --minify --target=bun-linux-x64 --outfile=supacloud-linux-amd64",
    "build:arm64": "bun build src/index.ts --compile --minify --target=bun-linux-arm64 --outfile=supacloud-linux-arm64",
    "build:macos-amd64": "bun build src/index.ts --compile --minify --target=bun-macos-x64 --outfile=supacloud-macos-amd64",
    "build:macos-arm64": "bun build src/index.ts --compile --minify --target=bun-macos-arm64 --outfile=supacloud-macos-arm64",
    "build:all": "bun run prebuild && bun run build:amd64 && bun run build:arm64 && bun run build:macos-amd64 && bun run build:macos-arm64"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/swagger": "^1.3.1",
    "elysia": "^1.4.25",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}