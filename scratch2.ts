import { Database } from "bun:sqlite";

// just try printing what the query looks like mathematically, but bun:sql is a tagged template.
// I just want to see if `bun:sql` silently fails. Let me just test syntax validity in sqlite
// wait, bun:sql is for postgres.
