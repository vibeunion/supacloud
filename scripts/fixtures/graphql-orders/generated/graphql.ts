// GENERATED BY @supacloud/compiler. DO NOT EDIT.
export type Maybe<T> = T | null;
export type InputMaybe<T> = Maybe<T>;
/** All built-in and custom scalars, mapped to their actual values */
export type Scalars = {
  ID: { input: string; output: string; }
  String: { input: string; output: string; }
  Boolean: { input: boolean; output: boolean; }
  Int: { input: number; output: number; }
  Float: { input: number; output: number; }
  /** A high precision floating point value represented as a string */
  BigFloat: { input: unknown; output: unknown; }
  /** An arbitrary size integer represented as a string */
  BigInt: { input: unknown; output: unknown; }
  /** An opaque string using for tracking a position in results during pagination */
  Cursor: { input: unknown; output: unknown; }
  /** A date without time information */
  Date: { input: unknown; output: unknown; }
  /** A date and time */
  Datetime: { input: unknown; output: unknown; }
  /** A Javascript Object Notation value serialized as a string */
  JSON: { input: unknown; output: unknown; }
  /** Any type not handled by the type system */
  Opaque: { input: unknown; output: unknown; }
  /** A time without date information */
  Time: { input: unknown; output: unknown; }
  /** A universally unique identifier */
  UUID: { input: unknown; output: unknown; }
};

/** Internal type. DO NOT USE DIRECTLY. */
type Exact<T extends { [key: string]: unknown }> = { [K in keyof T]: T[K] };
/** Internal type. DO NOT USE DIRECTLY. */
export type Incremental<T> = T | { [P in keyof T]?: P extends ' $fragmentName' | '__typename' ? T[P] : never };
export type OrderDetailQueryVariables = Exact<{
  id: number;
}>;


export type OrderDetailQuery = { ordersCollection: { edges: Array<{ node: { id: number, number: string, status: string, totalCents: number, customer: { id: number, name: string, email: string } | null, items: { edges: Array<{ node: { id: number, sku: string, description: string, quantity: number, unitPriceCents: number } }> } | null, deliveries: { edges: Array<{ node: { id: number, carrier: string, tracking: string, status: string } }> } | null } }> } };

function isGraphqlRecord(value: unknown): value is Record<string, unknown> {
  return value !== null && typeof value === "object" && !Array.isArray(value);
}
function isGraphqlArray(value: unknown): value is unknown[] {
  return Array.isArray(value);
}
function checkGraphqlValue0(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "ordersCollection") && checkGraphqlValue1(value["ordersCollection"]));
}
function checkGraphqlValue1(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "edges") && checkGraphqlValue2(value["edges"]));
}
function checkGraphqlValue2(value: unknown): boolean {
  return isGraphqlArray(value) && Array.from(value).every(checkGraphqlValue3);
}
function checkGraphqlValue3(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "node") && checkGraphqlValue4(value["node"]));
}
function checkGraphqlValue4(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "id") && checkGraphqlValue5(value["id"])) && (Object.prototype.hasOwnProperty.call(value, "number") && checkGraphqlValue6(value["number"])) && (Object.prototype.hasOwnProperty.call(value, "status") && checkGraphqlValue6(value["status"])) && (Object.prototype.hasOwnProperty.call(value, "totalCents") && checkGraphqlValue5(value["totalCents"])) && (Object.prototype.hasOwnProperty.call(value, "customer") && checkGraphqlValue7(value["customer"])) && (Object.prototype.hasOwnProperty.call(value, "items") && checkGraphqlValue10(value["items"])) && (Object.prototype.hasOwnProperty.call(value, "deliveries") && checkGraphqlValue15(value["deliveries"]));
}
function checkGraphqlValue5(value: unknown): boolean {
  return typeof value === "number" && Number.isFinite(value);
}
function checkGraphqlValue6(value: unknown): boolean {
  return typeof value === "string";
}
function checkGraphqlValue7(value: unknown): boolean {
  return checkGraphqlValue8(value) || checkGraphqlValue9(value);
}
function checkGraphqlValue8(value: unknown): boolean {
  return value === null;
}
function checkGraphqlValue9(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "id") && checkGraphqlValue5(value["id"])) && (Object.prototype.hasOwnProperty.call(value, "name") && checkGraphqlValue6(value["name"])) && (Object.prototype.hasOwnProperty.call(value, "email") && checkGraphqlValue6(value["email"]));
}
function checkGraphqlValue10(value: unknown): boolean {
  return checkGraphqlValue8(value) || checkGraphqlValue11(value);
}
function checkGraphqlValue11(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "edges") && checkGraphqlValue12(value["edges"]));
}
function checkGraphqlValue12(value: unknown): boolean {
  return isGraphqlArray(value) && Array.from(value).every(checkGraphqlValue13);
}
function checkGraphqlValue13(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "node") && checkGraphqlValue14(value["node"]));
}
function checkGraphqlValue14(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "id") && checkGraphqlValue5(value["id"])) && (Object.prototype.hasOwnProperty.call(value, "sku") && checkGraphqlValue6(value["sku"])) && (Object.prototype.hasOwnProperty.call(value, "description") && checkGraphqlValue6(value["description"])) && (Object.prototype.hasOwnProperty.call(value, "quantity") && checkGraphqlValue5(value["quantity"])) && (Object.prototype.hasOwnProperty.call(value, "unitPriceCents") && checkGraphqlValue5(value["unitPriceCents"]));
}
function checkGraphqlValue15(value: unknown): boolean {
  return checkGraphqlValue8(value) || checkGraphqlValue16(value);
}
function checkGraphqlValue16(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "edges") && checkGraphqlValue17(value["edges"]));
}
function checkGraphqlValue17(value: unknown): boolean {
  return isGraphqlArray(value) && Array.from(value).every(checkGraphqlValue18);
}
function checkGraphqlValue18(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "node") && checkGraphqlValue19(value["node"]));
}
function checkGraphqlValue19(value: unknown): boolean {
  return isGraphqlRecord(value) && (Object.prototype.hasOwnProperty.call(value, "id") && checkGraphqlValue5(value["id"])) && (Object.prototype.hasOwnProperty.call(value, "carrier") && checkGraphqlValue6(value["carrier"])) && (Object.prototype.hasOwnProperty.call(value, "tracking") && checkGraphqlValue6(value["tracking"])) && (Object.prototype.hasOwnProperty.call(value, "status") && checkGraphqlValue6(value["status"]));
}
export function isOrderDetailQuery(value: unknown): value is OrderDetailQuery {
  return checkGraphqlValue0(value);
}
export function parseOrderDetailQuery(value: unknown): OrderDetailQuery {
  if (!isOrderDetailQuery(value)) {
    throw new GraphqlRequestError("invalid-response", "GraphQL result does not match OrderDetail.");
  }
  return value;
}
export interface GraphqlQueryResults {
  "OrderDetail": OrderDetailQuery;
}
export function isGraphqlResult<Name extends keyof GraphqlQueryResults>(
  name: Name, value: unknown,
): value is GraphqlQueryResults[Name] {
  switch (name) {
    case "OrderDetail": return isOrderDetailQuery(value);
    default: return false;
  }
}
export function parseGraphqlResult<Name extends keyof GraphqlQueryResults>(
  name: Name, value: unknown,
): GraphqlQueryResults[Name] {
  if (!isGraphqlResult(name, value)) {
    throw new GraphqlRequestError("invalid-response", "GraphQL result does not match operation " + name + ".");
  }
  return value;
}

export type Requester<C> = (query: string, variables?: unknown, options?: C) => Promise<unknown>;
export function getSdk<C>(requester: Requester<C>) {
  return {
    async "OrderDetail"(variables: OrderDetailQueryVariables, options?: C): Promise<OrderDetailQuery> {
      return parseOrderDetailQuery(await requester("query OrderDetail($id: Int!) {\n  ordersCollection(filter: {id: {eq: $id}}, first: 1) {\n    edges {\n      node {\n        id\n        number\n        status\n        totalCents\n        customer {\n          id\n          name\n          email\n        }\n        items(first: 20, orderBy: [{id: AscNullsLast}]) {\n          edges {\n            node {\n              id\n              sku\n              description\n              quantity\n              unitPriceCents\n            }\n          }\n        }\n        deliveries(first: 10, orderBy: [{id: AscNullsLast}]) {\n          edges {\n            node {\n              id\n              carrier\n              tracking\n              status\n            }\n          }\n        }\n      }\n    }\n  }\n}", variables, options));
    }
  };
}

export interface GraphqlClientOptions {
  /** Project base URL, not the management API URL. HTTPS is required outside loopback. */
  url: string;
  /** Public project key only. Never put a service-role or management key in a browser. */
  publishableKey?: string;
  /** Resolved on every request so session refresh and logout are observed. */
  getAccessToken?: () => string | null | undefined | Promise<string | null | undefined>;
  fetch?: (url: string, init: RequestInit) => Promise<Response>;
}

export interface GraphqlRequestOptions {
  signal?: AbortSignal;
}

export class GraphqlRequestError extends Error {
  constructor(
    public readonly code: "http" | "graphql" | "invalid-response",
    message: string,
    public readonly status?: number,
    public readonly errors?: readonly unknown[],
  ) {
    super(message);
    this.name = "GraphqlRequestError";
  }
}

export function createGraphqlClient(options: GraphqlClientOptions) {
  const endpoint = new URL(options.url);
  const loopback = ["localhost", "127.0.0.1", "[::1]"].includes(endpoint.hostname);
  if (endpoint.protocol !== "https:" && !(endpoint.protocol === "http:" && loopback)) {
    throw new Error("GraphQL requires HTTPS outside loopback development.");
  }
  if (endpoint.username || endpoint.password || endpoint.search || endpoint.hash) {
    throw new Error("GraphQL project URLs must not contain credentials, query parameters or fragments.");
  }
  endpoint.pathname = endpoint.pathname.replace(/\/$/, "") + "/graphql/v1";
  const fetcher = options.fetch ?? globalThis.fetch.bind(globalThis);
  return getSdk<GraphqlRequestOptions>(async (
    query: string,
    variables?: unknown,
    request?: GraphqlRequestOptions,
  ): Promise<unknown> => {
    const headers = new Headers({ "Content-Type": "application/json", Accept: "application/json" });
    if (options.publishableKey) headers.set("apikey", options.publishableKey);
    const token = await options.getAccessToken?.();
    if (token) headers.set("Authorization", "Bearer " + token);
    const response = await fetcher(endpoint.toString(), {
      method: "POST",
      headers,
      body: JSON.stringify({ query, variables }),
      ...(request?.signal !== undefined ? { signal: request.signal } : {}),
      redirect: "error",
    });
    if (!response.ok) {
      throw new GraphqlRequestError("http", "GraphQL HTTP request failed (" + response.status + ").", response.status);
    }
    let envelope: unknown;
    try {
      envelope = await response.json();
    } catch {
      throw new GraphqlRequestError("invalid-response", "GraphQL returned invalid JSON.", response.status);
    }
    if (!envelope || typeof envelope !== "object" || Array.isArray(envelope)) {
      throw new GraphqlRequestError("invalid-response", "GraphQL returned an invalid envelope.", response.status);
    }
    if ("errors" in envelope) {
      if (!Array.isArray(envelope.errors)) {
        throw new GraphqlRequestError("invalid-response", "GraphQL returned invalid errors.", response.status);
      }
      if (envelope.errors.length > 0) {
        throw new GraphqlRequestError("graphql", "GraphQL query failed.", response.status, envelope.errors);
      }
    }
    if (!("data" in envelope) || !envelope.data || typeof envelope.data !== "object" || Array.isArray(envelope.data)) {
      throw new GraphqlRequestError("invalid-response", "GraphQL returned no result object.", response.status);
    }
    // The operation-specific parser in getSdk validates selected field values.
    return envelope.data;
  });
}
